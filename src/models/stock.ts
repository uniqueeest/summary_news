export type StockType = {
  name: string;
  ticker: string;
  category: string;
  country: string;
  label: string;
  value: string;
};

export type MarketType = {
  category: string;
  datetime: string;
  headline: string;
  image: string;
  id: string;
  summary: string;
};
